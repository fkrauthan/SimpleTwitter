/**
 * @jsx React.DOM
 */

var React = require('react');

var SimpleTwitterApp = React.createClass({
    render: function() {
        return (
            <div></div>
            );
    }
});

module.exports = SimpleTwitterApp;
